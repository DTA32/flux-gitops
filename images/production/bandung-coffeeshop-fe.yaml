apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: bandung-coffeeshop-fe
  namespace: flux-system
spec:
  image: ghcr.io/dta32/bandung-coffeeshop-fe/prod
  interval: 10m
  secretRef:
    name: flux-registry-creds
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: bandung-coffeeshop-fe
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: bandung-coffeeshop-fe
  filterTags:
    pattern: '^(?P<ts>[0-9]+)-[0-9a-f]{7}$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
