apiVersion: v1
kind: Service
metadata:
  namespace: prod
  name: job-scraper-mcp-service
spec:
  selector:
    app: job-scraper-mcp
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
