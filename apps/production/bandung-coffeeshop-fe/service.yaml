apiVersion: v1
kind: Service
metadata:
  namespace: prod
  name: bandung-coffeeshop-fe-service
spec:
  selector:
    app: bandung-coffeeshop-fe
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
