apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: prod
  name: postgres-postgis-pvc
spec:
  resources:
    requests:
      storage: 10Gi
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
