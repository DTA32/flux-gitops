apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: dev
  name: postgres-postgis-pvc
spec:
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
